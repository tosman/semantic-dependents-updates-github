machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - test $CIRCLE_BRANCH != master && rm .npmrc

test:
  pre:
    - npm run build
  post:
    - test -f .local-success && (npm run semantic-release || true)

